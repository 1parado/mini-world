# Supabase 图床配置
# 复制此文件为 .env.local 并填入你的真实值
# Vite 会自动加载 .env.local（不会被 git 提交）

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_BUCKET=photos

# 相册密码不再通过环境变量配置
# 请在游戏内「相册 → 上传照片」时设置，保存到浏览器 localStorage
