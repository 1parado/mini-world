# Supabase 图床配置
# 复制此文件为 .env.local 并填入你的真实值
# Vite 会自动加载 .env.local（不会被 git 提交）

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_BUCKET=photos

# 相册上传密码（非 VITE_ 前缀，构建时注入 SHA-256 哈希值到 JS，不明文暴露）
# 留空则不启用密码锁，任何人都可以上传
# 修改后需重新构建才能生效
ALBUM_PASSWORD=
